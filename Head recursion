Print 'N' natural numbers from 1 to n




#include <stdio.h>

void fun(int n)
{
    if(n>=1)
    {
        fun(n-1);
        printf("%d",n);
       
    }
}
int main()
{
    int n;
    scanf("%d",&n);
    fun(n);
}
