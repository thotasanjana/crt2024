Write a program for division of two numbers using if statement.


  #include <stdio.h>

int main() {
    int a,b;
    scanf("%d%d",&a,&b);
    if(b==0)
    {
        printf("Second no cannot be zero");
        b=1;
    }
    int r=a/b;
    printf("%d",r);

    return 0;
}
