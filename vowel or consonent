Read a char and printa char is vowel or not.?

#include <stdio.h>

int main() {
    char a;
    scanf("%c",&a);
    if(a=='a' || a=='e' || a=='i' || a=='0' || a=='u') 
    {
        printf("Vowel");
    }
    else
    {
        printf("consonant");
    }

    return 0;
}
