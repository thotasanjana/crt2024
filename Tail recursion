Print 'N' natural numbers from n to 1



#include <stdio.h>

void fun(int n)
{
    if(n>=1)
    {
        printf("%d",n);
        fun(n-1);
    }
}
int main()
{
    int n;
    scanf("%d",&n);
    fun(n);
}
